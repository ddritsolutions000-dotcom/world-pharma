import { JoinShell } from '../../src/join-shell';
import { JoinStatusPage } from '../../src/join-status-page';

export default function StatusPage() {
  return (
    <JoinShell>
      <JoinStatusPage />
    </JoinShell>
  );
}
