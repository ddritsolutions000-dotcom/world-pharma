import { DoctorShell } from '../../src/doctor-shell';

export default function Page() {
  return (
    <DoctorShell
      title="Settings"
      description="Security follows the identity kernel. Notifications are a placeholder."
    />
  );
}
