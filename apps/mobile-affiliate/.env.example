# Copy to .env.local (gitignored). Never commit secrets or a production API URL.

# local | sandbox | production
EXPO_PUBLIC_APP_ENV=local

# API used by shell-core apiFetch (same contract as customer mobile).
# EXPO_PUBLIC_API_BASE_URL=http://10.0.2.2:4000

# Public customer web origin for relative referral share links (required for sandbox/production).
# EXPO_PUBLIC_CUSTOMER_URL=https://customer.example.com
