/** Vendor seller onboarding routes (this site is vendor-only). */
export const VENDOR_JOIN_ROUTES = {
  hub: '/join',
  apply: '/join/apply',
  status: '/join/status',
} as const;

export const VENDOR_PARTNER_TYPE = 'VENDOR' as const;
